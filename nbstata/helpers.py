# AUTOGENERATED! DO NOT EDIT! File to edit: ../nbs/00_helpers.ipynb.

# %% auto 0
__all__ = ['InVar']

# %% ../nbs/00_helpers.ipynb 3
def InVar(code, # str: Stata 'in' specification
         ):
    """Return in-statement range"""    
    code = code.replace(' in ','').strip()
    slash_pos = code.find('/')
    if slash_pos == -1:
        return (None, None)
    start = code[:slash_pos]
    end = code[slash_pos+1:]
    if start.strip() == 'f': start = 1
    if end.strip() == 'l': end = count()
    return (int(start)-1, int(end))
